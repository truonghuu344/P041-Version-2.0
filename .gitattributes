# Shell scripts run inside Linux containers — CRLF breaks their shebang line
# (exec fails with "no such file or directory"). Force LF regardless of the
# checkout OS or a developer's core.autocrlf setting.
*.sh text eol=lf
